# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Release]
        c_compiler: [clang, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          - os: ubuntu-latest
            c_compiler: clang
            cpp_compiler: clang++
        exclude:
#          - os: windows-latest
#            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang


    env:
      BUILD_DIR: build
      CTEST_OUTPUT_ON_FAILURE: "1"
      JOBS: 2
      
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Set up ccache (Linux/macOS)
      if: runner.os != 'Windows'
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ccache-${{ runner.os }}-${{ hashFiles('**/CMakeLists.txt') }}
        restore-keys: |
          ccache-${{ runner.os }}-

    - name: Install Ninja on Linux
      if: runner.os == 'Linux'
      run: sudo apt-get update && sudo apt-get install -y ninja-build libgtest-dev libboost-system-dev libboost-program-options-dev libssl-dev libspdlog-dev libboost-all-dev

    - name: Install Ninja on macOS
      if: runner.os == 'macOS'
      run: brew install ninja

    - name: Install Ninja on Windows
      if: runner.os == 'Windows'
      run: choco install -y ninja 
      
    - name: Install Conan (all platforms)
      run: |
        python -m pip install --upgrade pip
        python -m pip install --upgrade conan

    - name: Conan profile detect (Linux/macOS)
      if: runner.os != 'Windows'
      shell: bash
      run: |
        conan --version
        echo "Existing profiles:"
        conan profile list || true
    
        # Try Conan 2 variant first, fall back to the simpler form if necessary
        conan profile detect --name default || conan profile detect || true
    
        # Show the default profile (try both possible argument forms)
        conan profile show --name default || conan profile show default || true

    - name: Conan profile detect (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        conan --version
        Write-Host "Existing profiles:"
        try { conan profile list } catch { Write-Host "profile list failed" }
    
        # Try Conan 2 variant, then fallback
        try {
          conan profile detect --name default
        } catch {
          try { conan profile detect } catch { Write-Host "profile detect failed, continuing..." }
        }
    
        # Show the default profile (try both possible forms)
        try {
          conan profile show --name default
        } catch {
          try { conan profile show default } catch { Write-Host "profile show failed" }
        }

    - name: Conan install dependencies
      run: |
        conan --version
        echo "Conan install into: ${{ steps.strings.outputs.build-output-dir }}"
        conan install . --output-folder "${{ steps.strings.outputs.build-output-dir }}" --build=missing -s build_type=${{ matrix.build_type }} -g CMakeToolchain -g CMakeDeps

    - name: Show generated Conan files (debug)
      run: |
        echo "Listing build dir:"
        ls -la "${{ steps.strings.outputs.build-output-dir }}" || true
        echo "Show conan_toolchain.cmake (if present):"
        if [ -f "${{ steps.strings.outputs.build-output-dir }}/conan_toolchain.cmake" ]; then
          echo "---- conan_toolchain.cmake ----"
          sed -n '1,200p' "${{ steps.strings.outputs.build-output-dir }}/conan_toolchain.cmake"
          echo "---- end ----"
        else
          echo "conan_toolchain.cmake not found"
        fi
      if: runner.os != 'Windows'
      shell: bash

    - name: Show generated Conan files (Windows debug)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        Write-Host "Listing build dir:"
        Get-ChildItem -Path "${{ steps.strings.outputs.build-output-dir }}" -Force -Recurse -ErrorAction SilentlyContinue
        $toolchain = Join-Path "${{ steps.strings.outputs.build-output-dir }}" "conan_toolchain.cmake"
        if (Test-Path $toolchain) {
          Write-Host "---- conan_toolchain.cmake ----"
          Get-Content $toolchain -TotalCount 200
          Write-Host "---- end ----"
        } else {
          Write-Host "conan_toolchain.cmake not found"
        }

    - name: Configure CMake (Linux/macOS - use Ninja)
      if: runner.os != 'Windows'
      shell: bash
      run: |
        cmake -B "${{ steps.strings.outputs.build-output-dir }}" \
          -G Ninja \
          -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }} \
          -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} \
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
          -DCMAKE_TOOLCHAIN_FILE="${{ steps.strings.outputs.build-output-dir }}/conan_toolchain.cmake" \
          -S "${{ github.workspace }}"

    - name: Configure CMake (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        cmake -B "${{ steps.strings.outputs.build-output-dir }}" `
          -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }} `
          -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} `
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} `
          -DCMAKE_TOOLCHAIN_FILE="${{ steps.strings.outputs.build-output-dir }}/conan_toolchain.cmake" `
          -S "${{ github.workspace }}"

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config ${{ matrix.build_type }}
